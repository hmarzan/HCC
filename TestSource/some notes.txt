1. Floating Point Compare : FIXED

what = dX == -2.0;


2. Relational operators fail  : FIXED 

  a. Pop from stack
  b. then assign

must be

 a. assign first
 b. pop from stack


3. Operator *= for Int64
4. Operator /= for Int64


5. Not supported this code ?

				char arr[10][20];

				string test = "Testing!!!";
				strncpy(arr[9], test, strlen(test));



6. Not assigning to globals : FIXED